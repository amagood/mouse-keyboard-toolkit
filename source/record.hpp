#ifndef RECORD_HPP
#define RECORD_HPP

void buildInputFileName(HWND hwnd,HWND *inputBar,HWND *enter,HWND *tips,HWND *tips2,HWND *inputBar2);
void record(int sleepTime,HWND hwnd);
int string2int(char * s);

#endif // RECORD_HPP

